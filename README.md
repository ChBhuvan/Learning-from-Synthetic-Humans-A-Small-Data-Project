# Learning-from-Synthetic-Humans-A-Small-Data-Project
Can a modern transformer-based architecture model, combined with targeted fine-tuning, loss functions, rich context labels, achieve strong part segmentation even when trained on only a small subset of the SURREAL Dataset?
